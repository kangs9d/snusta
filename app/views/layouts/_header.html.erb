<% if logged_in? %>

    <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href=<%= root_path %>>JUNASTA</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">

    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href=<%= root_path %>>HOME</a>
      </li>

      <li class="nav-item">
        <% if logged_in? %>
            <%= link_to 'PROFILE', profile_path(current_user.name), class: 'nav-link' %>
        <% else %>
            <%= link_to 'SIGN IN', new_user_path, class: 'nav-link' %>
        <% end %>
      </li>
      <li class="nav-item">
        <% if logged_in? %>
            <%= link_to "LOG OUT", :logout, method: :delete, class: 'nav-link' %>
        <% else %>

            <%= link_to 'LOG IN', :login, class: 'nav-link' %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
    <% else %>
<% end %>